# Runtime state — never ship in the tarball
server/data/**
server/certs/**
# Ship Silero VAD weights (required for trimSilence). Keep other large models out.
server/models/*
!server/models/silero_vad.onnx

# Dev-only
tsconfig.json
.gitignore
.cos-profile.json
.env
**/*.test.ts
